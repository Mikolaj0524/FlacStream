// Server port
export const PORT = 3001;

// Authentication
export const AUTHENTICATION = ["secret_key1", "secret_key2"];

/*
    AUTHENTICATION = null; - no auth
    AUTHENTICATION = ["secret_key1", "secret_key2"] - auth with keys
*/